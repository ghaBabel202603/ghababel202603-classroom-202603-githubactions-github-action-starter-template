name: 02 - Eventos
on: 
    - push
    - Pull_request
    - schedule:
        - cron: '15 4,5 * * *' 
jobs:
    echo: 
        runs-on: ubuntu-latest
        steps:
          - name: ShowTrigger
            run: | 
                echo "Evento que ha disparado el workflow: ${{ github.event_name }}"