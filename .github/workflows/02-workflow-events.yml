name: 02 - Eventos
on: 
    push:
    pull_request:
    schedule:
        - cron: "42 8 13 3 5"
    workflow_dispatch:
jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: ShowTrigger
              run: echo Event Name ${{ github.event_name }}